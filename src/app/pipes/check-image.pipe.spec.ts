import { CheckImagePipe } from './check-image.pipe';

describe('CheckImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckImagePipe();
    expect(pipe).toBeTruthy();
  });
});
